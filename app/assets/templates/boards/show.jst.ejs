hurray!

<div class="show_bg">

		<% board.get("lists").each(function(list){ %>
			<div class="show_list" data-id="list_<%= list.id %>">	
				<%= list.escape("title") %>
				<ul>
					<% list.get("cards").each(function(card){ %>

						<li>
							<div class="show_card" data-id="card_<%= card.id %>">
								<%= card.escape("title") %>
								<ul>
									<% card.get("tasks").each(function(task){ %>
										<div class="show_task" data-id="task_<%= task.id %>">
											<li> <%= task.escape("title") %></li>
										</div>
									<% }) %>							
								
								</ul>

							</div>
						</li>
					
					<% }) %>

				</ul>
			</div>
		<% }) %>	
</div>



<a href="">Back??</a>