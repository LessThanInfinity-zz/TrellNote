
    <h3>Welcome, <%= current_user.name%></h3>
    <ul>
      <% boards.each( function(board){ %>
        <li id="<%=board.id%>">
          <a href="#/boards/<%= board.id %>">
          <%= board.escape("title") %>
        </a>
        </li>
      <% }) %>
    </ul>