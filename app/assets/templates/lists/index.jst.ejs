<div class="lists_container" board-id="<%=board_id %>" >
	<% lists.each(function(list){ %>

		<div class="show list" 
					id="list_<%= list.id %>" 
					list-id="<%= list.id %>"> 

					
		  <div class="list_title">
		    <h5><%= list.escape("title") %></h5>
		  </div>

		  <% var cardIndexContent = JST["cards/index"]({cards: list.get("cards"), list_id: list.id, board_id: board_id }) %>
		  <!-- ^ refactor, but not priority -->  
		  <%= cardIndexContent %>

			<div class = "add_card_link">
			  <a href="#" 
			     id="add_card_link">
			    Add a Card...
			  </a>
			</div>

		</div>

	<% }) %>	


	<div class="show list"
				id="list_addNew">
		<a href='#' id="add_list_link">Add a List...</a>
	</div>
</div>